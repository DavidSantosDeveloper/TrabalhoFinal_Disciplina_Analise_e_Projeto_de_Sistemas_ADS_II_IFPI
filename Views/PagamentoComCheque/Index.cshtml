@model IEnumerable<FranciscoDavidSantosSousa.Models.PagamentoComCheque>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.banco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nomeDoBanco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nomeDoCobrado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.informacoesAdicionais)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotaDeVendaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discriminator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.banco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nomeDoBanco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nomeDoCobrado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.informacoesAdicionais)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotaDeVendaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discriminator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoDePagamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoDePagamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoDePagamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
