@model IEnumerable<FranciscoDavidSantosSousa.Models.Vendedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotaDeVendaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotaDeVendaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVendedor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdVendedor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVendedor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
